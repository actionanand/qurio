# Android releases

The `main-android` GitHub Actions workflow writes the current Qurio APK, AAB, R8 mapping, and Play Store icon into this directory. Signed files omit the `-unsigned` suffix; unsigned fallbacks are clearly labeled.

Keep each mapping file with its matching Play Store release. Do not place signing keys or passwords here.
